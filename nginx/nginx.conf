http {
    include mime.types;

    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
        }
        location /api/auth {
            proxy_pass http://rag-microservice:3000;
        }
        location /socket.io/ {
            proxy_pass http://rag-microservice:3000;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_http_version 1.1;
            proxy_set_header Host $host;
        }
    }
}

events {}